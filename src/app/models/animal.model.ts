import { Imagem } from "./imagem.model";

export class Animal  {
  
    nome: string;
    porte: string;
	genero: string;
	idRaca: number;
	cor: string;
	descricao: string;
	idCidade: number;
	imagens: Imagem[];

    constructor(
   
    ) {}
}